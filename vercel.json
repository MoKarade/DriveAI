{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd app && npm ci && npm run build",
  "outputDirectory": "app/dist",
  "framework": null,
  "installCommand": "true",
  "rewrites": [
    { "source": "/hub/summary", "destination": "/api/hub-summary" }
  ]
}
