# État local de la boucle de leçons (marqueurs de session)
.claude/.state/

# Identifiants clasp (Apps Script) — NE JAMAIS committer
.clasprc.json
.clasp.json

# Secrets / environnement
.env
.env.*
*.key
*.pem

# Node / app web (Phase 4)
node_modules/
dist/
.vite/
*.local

# OS / éditeurs
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# E2E Playwright (captures d'écran CI) — artefacts locaux, remontés en artifact GitHub
app/e2e-screenshots/
app/e2e-results/
